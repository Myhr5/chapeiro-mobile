import {colors, font_size} from "../../constants/theme"

export const styles = {
    header: {
        width: "100%",
        alignItems: "center"
    },
    logo: {
        width: 170,
        height: 50
    },
    title: {
        color: colors.dark_gray,
        fontSize: font_size.md,
        textAlign: "center"
    }
}