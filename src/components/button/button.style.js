import {colors, font_size} from "../../constants/theme"

export const styles = {
      btn: {
            width: "100%",
            backgroundColor: colors.yellow,
            borderRadius: 6
      },
      text: {
            fontSize: font_size.md,
            color: colors.white,
            padding: 14,
            textAlign: "center"
      }
}